"""Database seeding package.

Contains scripts that populate reference/static data (venues, teams, etc.) into
the database. The :mod:`app.seeds.seed_all` entrypoint orchestrates the
per-sport seeders found under ``app/seeds/sports/``.
"""
