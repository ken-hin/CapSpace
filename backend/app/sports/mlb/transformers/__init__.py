"""MLB transformers package.

Will hold MLB-specific transformer implementations that normalize raw provider
payloads (MLB Stats API, Statcast) into MLB model-ready dictionaries, conforming
to the :class:`~app.ingestion.transformers.base_transformer.BaseTransformer` interface.
"""
