"""REST API package.

Contains the FastAPI ``APIRouter`` modules that define the platform's HTTP
endpoints (players, games, predictions, stats) and the aggregate
:mod:`app.api.router` that mounts them under the ``/api`` prefix.
"""
