"""Top-level package for the CapSpace ML pipeline.

Contains the offline machine-learning workflow that produces pre-game
predictions: feature engineering (:mod:`src.features`), model definitions
(:mod:`src.models`), the training entrypoint (:mod:`src.training`), and the
prediction/upload entrypoint (:mod:`src.predict`).
"""
